/*
 * @file plot3d.hpp
 * @brief Re-export of ImPlot3D for 3D charting inside QuickViz panels
 *
 * Use this header when you want to draw a 3D chart (scatter, surface, mesh,
 * lines) directly into an ImGui window via ImPlot3D's immediate-mode API.
 * Distinct from the `scene` module: `scene` builds a persistent, interactive
 * 3D world; `plot3d` draws a 3D *chart* of data inside an ImGui window.
 *
 * Copyright (c) 2026 Ruixiang Du (rdu)
 */

#ifndef QUICKVIZ_PLOT_PLOT3D_HPP
#define QUICKVIZ_PLOT_PLOT3D_HPP

#include "implot3d/implot3d.h"

#endif  // QUICKVIZ_PLOT_PLOT3D_HPP
